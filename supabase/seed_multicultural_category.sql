-- =============================================================================
-- "다문화" 카테고리 신설 (7번째 카테고리)
--
-- 생애주기 6개 카테고리(결혼~어르신) 다음에 배치합니다. 특정 생애주기가
-- 아니라 국내 체류 외국인·다문화가족이라는 대상군을 기준으로 하는
-- 카테고리이므로 sort_order 7로 맨 뒤에 추가합니다.
-- =============================================================================

insert into categories (slug, name, description, sort_order) values
  ('multicultural', '다문화', '국내 체류 외국인과 다문화가족을 위한 지원 제도', 7)
on conflict (slug) do nothing;
